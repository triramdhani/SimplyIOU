import React, { ReactNode } from 'react'

interface IActiveContract {
  children: ReactNode
}

const ActiveContract = ({children}:IActiveContract) => {
  return (
    <div>
      
    </div>
  )
}

export default ActiveContract
